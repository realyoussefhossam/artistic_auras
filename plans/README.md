# Implementation Plans — Artistic Auras

Generated by the improve skill on 2026-07-19. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix sold-out detection in MintButton | P1 | S | — | DONE |
| 002 | Add wrong-network guard and chain-aware URLs | P1 | M | — | DONE |
| 003 | Sync royalty receiver with ownership transfers | P1 | S | — | DONE |
| 004 | Wire up real token ID in MintSuccessModal | P2 | S | — | DONE |
| 005 | Add frontend CI (build, lint, typecheck) | P2 | S | — | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- No hard dependencies between these 5 plans — all can be executed in parallel.
- Suggested order: 001 and 003 are the quickest wins. 002 touches the most files. 005 should land before any future test-coverage plan.

## Findings considered and rejected

- **Image rename non-atomicity**: `shutil.move` within the same filesystem is a single rename; not a practical risk.
- **Case-insensitive image matching collision**: current filenames are unique numeric stems, so no collision exists.
- **`_tokenIds` would overstate supply if burning were added**: there is no burn function today; this is a future-extension note, not a live bug.
- **StatsBar shows fake data on landing page** (finding #5): deferred — it's a cosmetic issue on the landing page, not a functional bug. Can be addressed in a future plan.
- **Mint page false advertising copy** (finding #6): deferred — this is a copywriting decision for the project owner, not a code fix.
- **MAINNET_CONTRACT is zero address** (finding #10): folded into plan 002 — the wrong-network guard makes this moot until mainnet deployment.
- **Zero frontend tests** (finding #9): deferred — needs plan 005 (frontend CI) to land first.
- **README doesn't mention frontend** (finding #11): low priority, deferred.
- **Dead code in lib/config.ts**: `NEXT_PUBLIC_CHAIN_ID`, `defaultChain`, `chains`, and `wcProjectId` are all unused. Cleanup folded into plan 002.
